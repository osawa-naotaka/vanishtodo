import type { JSX } from "react";

export function Home(): JSX.Element {
    return (
        <div>
            <h1>Welcome to VanishTodo</h1>
            <p>Your tasks, vanished when you need them gone.</p>
        </div>
    );
}
