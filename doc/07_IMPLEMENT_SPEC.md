
実装仕様


1. local storageにはstringしか格納できない。tasksのjsonをparseして格納している。壊れたlocal storageから読み込むのを防ぐためにvalibotを用いている。

2. types.tsに乗せる共通に使う型はできる限りvalibotでスキーマを定義しておく


3. エラーハンドリング
エラー発生時も、発生を通達はするがデータはローカルのものを使って処理を続けたいという要求がある。
そのため、VResp<T>型を用意する。エラーステータスと返り値を両方設定できるようにする。