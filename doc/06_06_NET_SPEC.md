# ネット層機能仕様

## 概要

ネット層は、永続化層からの要求を受け、DB層へのHTTPリクエストを生成し、レスポンスを解析して永続化層に返します。
ネット層を作った理由は、永続化層で使うネット層の実体をDIに類似した手法で注入して、テスト容易性を上げるためです。
解析するレスポンスは共通部分のみで、ボディ内の個別のデータ部分に関しては解析せずunknown型になります。
個別データ部分はvalibotでパースする前提です。
